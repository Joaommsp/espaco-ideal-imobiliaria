/**
 * O globo é quadrado e o container do hero recorta o que passa embaixo. A
 * órbita de cidades precisa da mesma medida para achar o centro da esfera —
 * por isso o número mora aqui, e não dentro de um dos dois componentes.
 */
export const LADO_DO_GLOBO = 620;

/** Inclinação do eixo, compartilhada para o desenho e a projeção. */
export const INCLINACAO_DO_EIXO = 0.25;
